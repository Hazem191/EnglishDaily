# NOTE: Production deployment should use Apache + PHP (api.php) for data sync.
# This Render config is for optional Node static hosting only — sync features
# require api.php. See README.md for PHP deployment instructions.

services:
  - type: web
    name: daily5minutesenglish
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
